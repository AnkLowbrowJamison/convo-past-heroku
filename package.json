{
    "name": "conversations-with-the-past",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "client-dev": "cd client && vite",
        "client-build": "cd client && vite build",
        "server-dev": "nodemon server/server",
        "dev": "concurrently \"npm run server-dev\" \"npm run client-dev\"",
        "build": "npm run client-build",
        "start": "node server/server"
    },
    "dependencies": {
        "axios": "^0.26.1",
        "cors": "^2.8.5",
        "dotenv": "^16.0.3",
        "express": "^4.18.2",
        "nodemon": "^2.0.22",
        "openai": "^3.2.1"
    },
    "devDependencies": {
        "concurrently": "^7.6.0",
        "vite": "^4.3.2"
    }
}
