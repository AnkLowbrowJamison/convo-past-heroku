{
    "name": "conversations-with-the-past",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "server": "nodemon server",
        "client": "npm run --prefix ../client build",
        "build": "npm install && npm run client",
        "start": "concurrently \"npm run server\" \"npm run --prefix ../client dev\""
    },
    "dependencies": {
        "concurrently": "^7.6.0",
        "cors": "^2.8.5",
        "dotenv": "^16.0.3",
        "express": "^4.18.2",
        "nodemon": "^2.0.22",
        "openai": "^3.2.1"
    }
}
